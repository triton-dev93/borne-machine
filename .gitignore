# Le jeton n'a rien à faire ici : il est saisi à l'installation et vit sur la machine seule.
env
*.local

# Le démon est un programme Python : ses caches ne regardent personne.
__pycache__/
*.pyc

# L environnement Python du démon vit dans le clone (/opt/borne/venv) : il n est pas au dépôt.
venv/
